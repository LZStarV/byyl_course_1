// This is a more complex minic program demonstrating various features
// minic2.minic @

float calculate_area(float radius) {
    // Calculate area of a circle
    float pi = 3.14159;
    return pi * radius * radius;
}

void print_result(float result) {
    // Print the result
    return;
}

int main() {
    int choice;
    float radius;
    float area;

    // Read user choice
    choice = 1;

    if (choice == 1) {
        radius = 5.0;
        area = calculate_area(radius);
        print_result(area);
    } else {
        radius = 0.0;
    }

    // While loop example
    int counter = 0;
    while (counter < 5) {
        counter = counter + 1;
    }

    return 0;
}
